# cs35l-project-w26
## Week1
### Backend D
- [x] Add `Flask-SQLAlchemy` and `Flask-Migrate` dependencies to `requirements.txt`
- [x] Create `models/` folder
- [x] Initialize SQLAlchemy with the Flask app (`db.init_app(app)`)
- [x] Create 1 test model
- [x] Generate the first migration (`flask db init` & `flask db migrate`)
- [ ] Apply the migration to the database (`flask db upgrade`) => requires DATABASE_URL